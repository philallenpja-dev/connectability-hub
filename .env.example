# Set to your deployed Payload CMS URL to make the app fetch live data
# from Payload instead of using MOCK_* arrays. Leave blank for mocks-only.
VITE_CMS_URL=https://connectability-hub-cms.vercel.app
